# ihcafe_docs: IHCAFE, PROMECAFE, WCR, ICO and Honduran press documents

**Status (2026-09-25): empty.** None of the 49 requested URLs could be downloaded. The session's egress proxy refused all 14 source hosts with `403`, so no raw files and no `.txt` extractions exist yet. See `FETCH_LOG.md` for each URL and its result, and for the list of domains to allow.

No figures on varieties, area by variety, credit amounts, costs or farm-gate prices were collected from these sources. Nothing in this folder should be cited until the files are fetched.

| File | What it is | Date | Key numbers (page) |
|---|---|---|---|
| `FETCH_LOG.md` | Fetch log: HTTP result for each URL and the blocked hosts | 2026-09-25 | n/a |
